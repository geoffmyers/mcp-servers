{
  "name": "@geoffmyers/mcp-server-truenas",
  "version": "1.0.0",
  "description": "MCP server for TrueNAS SCALE WebSocket API",
  "license": "GPL-3.0-or-later",
  "author": "Geoff Myers",
  "type": "module",
  "bin": {
    "mcp-server-truenas": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx mkdir -p dist/docs && shx cp -r docs/* dist/docs/ && shx chmod +x dist/index.js",
    "watch": "tsc --watch",
    "start:stdio": "node dist/index.js stdio",
    "start:streamableHttp": "node dist/index.js streamableHttp",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@geoffmyers/mcp-server-shared": "file:../mcp-server-shared",
    "@modelcontextprotocol/sdk": "^1.25.2",
    "ws": "^8.18.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "shx": "^0.3.4",
    "typescript": "^5.6.2",
    "vitest": "^3.0.0"
  }
}
