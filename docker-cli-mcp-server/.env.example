# Example configuration. Copy to .env and fill in your own values.
# Angle-bracket values are placeholders; replace them.
# Docker CLI MCP Server Configuration
# EXECUTION_MODE: "local" to run docker commands directly, "ssh" to run via SSH
EXECUTION_MODE=ssh
SSH_HOST=<your-ssh-host>
